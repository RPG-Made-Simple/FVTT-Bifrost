/**
 * Main module class.
 */
export declare class Bifrost {
    /**
     * Initializes the module, everything that is essential to the module is
     * first initialized here.
     */
    static initialize(): void;
}
